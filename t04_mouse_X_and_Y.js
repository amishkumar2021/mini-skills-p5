/********************************************************/
//
/********************************************************/
function setup() {
  createCanvas(400, 400);
  background(220);
  background("red")
}

function draw() {
  noStroke();
  fill (50,500, 250, 50)
  ellipse(mouseX, mouseY, 65, 100);
}